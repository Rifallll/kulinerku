// Large cooking knowledge dataset generated programmatically
// This file creates 1000 placeholder topics for AI Chef.
export const LARGE_KNOWLEDGE: { [key: string]: string } = {};
for (let i = 1; i <= 1000; i++) {
    const key = `topic${i}`;
    const value = `🧠 Jawaban otomatis untuk topik ${i}: informasi kuliner umum atau tips memasak yang dapat dikembangkan lebih lanjut.`;
    LARGE_KNOWLEDGE[key] = value;
}
